import {useState} from "react"

export const Navbar=()=>{


    return (
        <div id="navbar">
            <input type="text" placeholder="search.." />
            <button className="search">Search</button>
        </div>
    )

}